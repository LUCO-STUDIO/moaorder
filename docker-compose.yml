services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: moaorder
      POSTGRES_PASSWORD: moaorder
      POSTGRES_DB: moaorder
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
